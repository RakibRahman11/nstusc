const SectionTitle = ({ heading }) => {
    return (
        <div>
            <p className="py-5 text-xl font-bold text-center uppercase md:text-3xl text-[#223c7f]">{heading}</p>
        </div>
    );
};

export default SectionTitle;